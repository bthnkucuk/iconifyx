// Placeholder library — the render host is driven by the test file in
// `test/render_icon_test.dart`. This file exists so the package has a
// non-empty `lib/` for `flutter pub get`.
library render_host;
