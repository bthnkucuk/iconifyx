name: two_icon_test
description: Verify bundle size when an app depends on only two per-set packages.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.3.0
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  iconifyx_core:
    path: ../../packages/iconifyx_core
  iconifyx_mdi:
    path: ../../packages/iconifyx_mdi
  iconifyx_lucide:
    path: ../../packages/iconifyx_lucide

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
