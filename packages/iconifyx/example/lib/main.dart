// Hand-written entry point. All UI lives in `app.dart`, which is regenerated
// from `tools/generator/templates/example_app.dart` on every `bun run generate`.
// Edit the template if you want to change the example UI.

import 'package:flutter/material.dart';

import 'app.dart';

void main() => runApp(const IconifyxExampleApp());
