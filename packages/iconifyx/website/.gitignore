# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# CDN tree (RESEARCH_PLAN §11/§12). Regenerated by
# `bun run generate` into lib/cdn/{packs,icons-index}/v1/. Tracked as
# a build artifact rather than source: the data is re-derivable from
# the generator + @iconify/json version, and committing 10 MB of JSON
# duplicates what's already in lib/data/{packs,icons_index}.json.
#
# Will be committed under a release tag (e.g. `iconify-2.2.500`) when
# we flip `kUseCdn = true` in lib/bootstrap/icon_catalog.dart and point
# jsDelivr at that ref.
/lib/cdn/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
