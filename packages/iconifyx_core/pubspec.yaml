name: iconifyx_core
description: >-
  Shared core types for the iconifyx family — IconifyIconData wrapper and
  license metadata. Imported by every iconifyx_* per-set package.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.3.0
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
