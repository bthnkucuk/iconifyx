// GENERATED FILE — do not edit.
// Source: @iconify/json v2.2.472, set "solar"
// Regenerate via `bun run generate --set solar`.

import 'package:iconifyx_core/iconifyx_core.dart';

import 'src/sets/solar.dart';

/// Alias map for the "solar" pack (6 entries).
///
/// Pure-rename aliases share their canonical's codepoint, so this map
/// is purely a Dart-side convenience — every value points at the matching
/// const on the main `SolarIcons` class.
///
/// Importing this file is opt-in. Apps that only need a few canonical
/// icons should import the main library instead; the tree shaker drops
/// every unused canonical from the bundled TTF. Pulling this map in
/// retains every referenced canonical — which is the correct behaviour
/// for browse / search use cases.
const Map<String, IconifyIconData> solarAliases = {
  'clound-cross-bold': SolarIcons.cloudCrossBold,
  'clound-cross-bold-duotone': SolarIcons.cloudCrossBoldDuotone,
  'clound-cross-broken': SolarIcons.cloudCrossBroken,
  'clound-cross-line-duotone': SolarIcons.cloudCrossLineDuotone,
  'clound-cross-linear': SolarIcons.cloudCrossLinear,
  'clound-cross-outline': SolarIcons.cloudCrossOutline,
};
